events {
  worker_connections 1024;
}

http {
  server {
    listen {{ nginx_listen_port }} default_server;
    server_name default_server_1;
    root /usr/share/nginx/html;
    location / {
    }
  }
}
